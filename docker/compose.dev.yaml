# Creates development Database
name: "registration_dev"
services:
  postgres:
    image: "postgres:16-alpine"
    container_name: "registration_dev_postgres"
    restart: always
    ports:
      - "5431:5432"
    networks:
      - registration_dev_network
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      TZ: "Europe/Bratislava"
      PGTZ: "Europe/Bratislava"
      POSTGRES_USER: horizon_user
      POSTGRES_PASSWORD: horizon_pass
      POSTGRES_DB: horizon
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $$POSTGRES_DB -U $$POSTGRES_USER"]
      interval: 1s
      timeout: 5s
      retries: 10
volumes:
  postgres_data:
    name: registration_dev_data
networks:
  registration_dev_network:
    name: registration_dev_network
    driver: bridge